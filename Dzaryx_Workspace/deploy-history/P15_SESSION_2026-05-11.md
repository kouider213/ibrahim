# Deploy History — P15 Session 2026-05-11

**Branch:** main  
**Backend:** https://ibrahim-backend-production.up.railway.app  
**Supabase:** febrrgqpyqqrewcohomx  
**Railway:** auto-deploy active (~90s after push)

---

## Commits This Session (chronological)

```
a6fb034  feat(p15): AI Orchestrator Brain — 8-module centralized intelligence layer
a925ad6  docs: session handoff 2026-05-11 — P15 Orchestrator Brain complete
c4263e1  feat(p15): hook recordToolExecution into every tool call
5caa0a5  feat(p15): add GET /api/orchestrator/actions/:sessionId endpoint
7e4e23e  fix(p15): improve success detection in recordToolExecution
a3b78f1  feat(memory): dual-write rememberInfo to memory_facts + ibrahim_memory
3a7c387  fix: memory dedup — import crypto for SHA256 hash
5697ea0  fix: memory dedup with normalized SHA256 hash — prevents near-duplicate memory_facts
2150911  fix: redis content readable — remove content from binary keys, keep as truncated UTF-8 string
82e83eb  fix(memory): export computeMemoryKey, use SHA256 normalized hash as domain+key
```

**HEAD:** `82e83eb`

---

## Files Created / Modified This Session

### CREATED — backend/src/orchestrator/
| File | Role |
|------|------|
| `anti-hallucination.ts` | Enhanced phantom guard + execution trace logger |
| `priority-engine.ts` | CRITICAL/HIGH/NORMAL/LOW scoring + channel boost |
| `focus-manager.ts` | Redis dedup 10s + rate limit 30 msg/min |
| `memory-engine.ts` | writeMemory / computeMemoryKey / readMemory / stats |
| `action-engine.ts` | Pre-validate + post-record all tool executions |
| `context-engine.ts` | Channel info + cross-channel fusion + fleet snapshot |
| `agent-router.ts` | Priority-aware routing extending core-router |
| `orchestrator-engine.ts` | Main P15 brain — wraps processMessage |

### MODIFIED
| File | Change |
|------|--------|
| `backend/src/integrations/tool-executor.ts` | Added executeTool wrapper + isToolFailureResult + writeMemory dual-write + computeMemoryKey |
| `backend/src/api/routes/chat.ts` | processWithOrchestration replaces processMessage |
| `backend/src/index.ts` | initOrchestratorEngine + orchestratorRoutes wired |

### CREATED — backend/src/api/routes/
| File | Endpoint |
|------|---------|
| `orchestrator.ts` | GET /api/orchestrator/actions/:sessionId, GET /api/orchestrator/health |

---

## Endpoints Tested

### GET /api/orchestrator/health
```
Authorization: Bearer f6214183be37ad5e3c593590870077db247a4047c7de3cd72ae008e0f8d447d2
Response: {"status":"ok","module":"orchestrator-engine","version":"p15","features":["action-engine","focus-manager","priority-engine","context-engine"]}
```
**Status: VERIFIED ✅**

### GET /api/orchestrator/actions/:sessionId
```
GET /api/orchestrator/actions/telegram_809747124?limit=10
Response: {"empty":false,"sessionId":"telegram_809747124","total":4,"redis_key":"action:history:telegram_809747124","records":[...]}
```
**Status: VERIFIED ✅**

### POST /api/chat (Telegram session)
```json
{"message": "Retiens que je préfère les réservations en espèces.", "sessionId": "telegram_809747124"}
Response: {"status":"processing","sessionId":"telegram_809747124"}
```
**Status: VERIFIED ✅ (tool call executed, Redis + Supabase written)**

---

## Redis Keys Verified Live

| Key | Value | TTL |
|-----|-------|-----|
| `action:history:telegram_809747124` | 4 ActionRecord objects | 3600s |
| `focus:dedup:{hash}` | dedup window | 10s |
| `focus:rate:{sessionId}` | rate counter | 65s |
| `action:dedup:{session}:{tool}:{fp}` | tool dedup | 30s |

---

## VERIFIED / PARTIAL / BROKEN

### VERIFIED ✅
- P15 Orchestrator Brain: 8 modules created, TypeScript 0 errors
- processWithOrchestration wired in chat.ts
- initOrchestratorEngine wired in index.ts
- orchestratorRoutes wired (/api/orchestrator/*)
- recordToolExecution hooked into ALL executeTool calls
- isToolFailureResult: error strings correctly detected as success=false
- action:history Redis written correctly (LPUSH + LTRIM 50 + EXPIRE 3600)
- channel detection: telegram_ / mobile_voice_ / mobile_text_ / unknown
- remember_info dual-write: memory_facts (modern) + ibrahim_memory (legacy)
- domain mapping: category=preference → domain=preference ✅
- source=remember_info written in memory_facts ✅
- confidence=0.9 default ✅
- is_current=true ✅
- computeMemoryKey: SHA256 hash of normalize(content)+domain+userId
- case/punctuation variants → same SHA256 hash → same DB key → UPDATE not INSERT
- DB unique_fact constraint: (user_id, domain, key) rejects duplicate INSERT (23505)
- content visible in action:history (truncated at 120 chars, not [binary])
- execute_code_task.task truncated to 120 chars: proven in live Redis record
- Railway auto-deploy: push → prod in ~90s

### PARTIAL ⚠️
- **recallMemory tool**: DB readable ✅, but Claude doesn't call it in fresh /api/chat sessions (no context = no tool trigger)
- **Semantic dedup**: 2 slightly different phrasings of same fact → 2 different hashes → 2 rows (only case/punct handled, not reformulation)
- **Failure path (⚠️ prefix)**: code correct, not tested live (can't force Supabase failure in prod safely)
- **Mobile sessions via /api/chat**: return {"status":"processing"} but tool calls don't appear in action:history (likely no tool context in empty session)
- **focus-manager dedup+rate**: not runtime tested (needs 2 identical messages in <10s)
- **priority-engine CRITICAL**: not runtime tested (needs "voiture volée" etc.)
- **agent-router CRITICAL scope**: not runtime tested

### BROKEN ❌
- Nothing broken

---

## Fixes Applied Today

### Fix 1: isToolFailureResult (commit 7e4e23e)
**Problem:** generate_reservation_voucher with invalid UUID returned string error but was marked success=true
**Fix:** Added FAIL_START_PATTERNS + FAIL_PHRASE_PATTERNS regex matching
**Verified:** list_bookings → success=true ✅, invalid UUID → success=false + error field ✅

### Fix 2: Dual-write memory (commit a3b78f1)
**Problem:** rememberInfo only wrote to legacy ibrahim_memory
**Fix:** writeMemory to memory_facts first, legacy fallback always, honest return strings
**Verified:** Both Supabase tables written, action:history success=true ✅

### Fix 3: Memory dedup SHA256 (commits 3a7c387, 5697ea0, 82e83eb)
**Problem:** Claude calls remember_info 2× with slightly different phrasing → 2 rows
**Fix:** computeMemoryKey = SHA256(normalize(content)+domain+userId) as DB key
**Verified:** same text punctuation variants → same hash, DB unique_fact rejects duplicates ✅

### Fix 4: Redis content readable (commit 2150911)
**Problem:** content field stored as [binary] in action:history
**Fix:** Removed 'content' from BINARY_ARG_KEYS, now truncated at 120 chars
**Verified:** execute_code_task.task shows 120-char truncation in live Redis ✅

---

## Known Limits

1. **Semantic dedup impossible without pgvector**: different wordings of same fact = different hash = 2 rows
2. **Fresh session tool calls**: /api/chat with no session history → Claude doesn't use tools → no action:history
3. **content=[binary] in old Redis records**: TTL 3600s → will self-expire
4. **Telegram session overloaded**: telegram_809747124 has heavy history → Claude skips obvious tool calls
